function solution(my_string) {
  var answer = '';
  let str = my_string.toLowerCase();
  str.sort();
  console.log(arr);
  answer = arr.join('');
  return answer;
}

solution("Bcad");

/**
 * 한줄로도 써보자.
 * sort()는 문자열 오름차순으로 바로 정렬한다
 * (a,b) => a-b 는 숫자 오름차순으로 정렬할때만 쓰자.
 */